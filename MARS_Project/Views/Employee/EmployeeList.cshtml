@model IEnumerable<MARS_Project.Models.User>
@{
    ViewBag.Title = "Employee List";
    Layout = "~/Views/Shared/_LayoutEmployee.cshtml";
}

<h3 class="fw-bold mb-4 text-center text-primary">
    <i class="fa fa-user-tie"></i> Employee List
</h3>

<table id="employeeTable" class="display nowrap table table-bordered table-hover align-middle shadow-sm" style="width:100%">
    <thead>
        <tr>
            
            <th>Name</th>
            <th>Email</th>
            <th>Contact</th>
            <th>Address</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var e in Model)
        {
            <tr>
               
                <td>@e.Name</td>
                <td>@e.Email</td>
                <td>@e.ContactNumber</td>
                <td>@e.Address</td>
                <td>
                    @if (e.Status == "Active" || e.Status == "Approved")
                    {
                        <span class="badge bg-success">Active</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">Inactive</span>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#employeeTable').DataTable({
                dom: '<"d-flex justify-content-between align-items-center mb-2"Bf>rtip',
                buttons: [
                    { extend: 'copy', text: '<i class="fa fa-copy"></i> Copy' },
                    { extend: 'csv', text: '<i class="fa fa-file-csv"></i> CSV' },
                    { extend: 'excel', text: '<i class="fa fa-file-excel"></i> Excel' },
                    { extend: 'pdf', text: '<i class="fa fa-file-pdf"></i> PDF' },
                    { extend: 'print', text: '<i class="fa fa-print"></i> Print' }
                ],
                responsive: true,
                language: { search: "_INPUT_", searchPlaceholder: "Search employees..." }
            });
        });
    </script>
}
